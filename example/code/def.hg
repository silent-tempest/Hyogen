
# bug: block below was incorrectly parsed
times ( times, callback )
  for i = 0 to times
    callback # ( i )

# this 'add' missed
add ( a, b ) =>
  a + b

greet name
  print 'Hello, ' + name + '!'

sq x =>
  x * x

print 'sq.length = ' + sq.length
